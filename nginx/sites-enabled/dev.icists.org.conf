server {
    listen      80;
    server_name dev.icists.org;
    charset      utf-8;

    location / {
        return 301 https://dev.icists.org$request_uri;
    }
}

server {
    listen       443;
    server_name  dev.icists.org;
    charset      utf-8;

    client_max_body_size 16M;

    ssl                  on;
    ssl_certificate      /etc/certs/dev.icists.org.crt;
    ssl_certificate_key  /etc/certs/dev.icists.org.key;
    ssl_session_timeout  5m;
    ssl_protocols  SSLv2 SSLv3 TLSv1;
    ssl_ciphers 'EECDH+AESGCM:EDH+AESGCM:AES256+EECDH:AES256+EDH';
    ssl_prefer_server_ciphers   on;
    location ~ /\.ht {
        deny  all;
    }

    location /static {
        alias /static;
    }

    location / {
        proxy_pass http://web-vue:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
